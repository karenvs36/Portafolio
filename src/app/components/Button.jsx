import React from "react";

const Button = ({ ImageUrl, linkUrl, altText, className }) => {
  return (
   /* <a href="{linkUrl}" target="_blank" rel="noopener noreferrer">
      <img
        src={imageUrl}
        alt={altText}
        className={`cursor-pointer ${className}`}
      />
    </a>*/
    <div>
      
    </div>
  );
};

export default Button;
